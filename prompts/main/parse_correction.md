The goal file you just edited failed to parse:

{LISTING}

Read the file, identify the structural error (typical causes: unclosed `"""`, a duplicate top-level key, or content placed inside the description block instead of after its closing `"""`), and rewrite the full file with the Write tool. The file must have these top-level keys in order: {KEYS_ORDER}.