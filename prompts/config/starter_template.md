# Model configuration for tinker. Uncomment and edit a slot to override
# the built-in default for that tier. Commenting a line out reverts to
# the built-in default. Unrecognized model names pass through to the
# backend as-is.

# Tinker talks to OpenRouter directly; slots are OpenRouter model ids.
# The OPENROUTER_API_KEY environment variable must be set.
[native]
# high = "{NATIVE_HIGH}"  # tend, rummage, jog — built-in default
# mid  = "{NATIVE_MID}"  # goal sessions — built-in default
# low  = "{NATIVE_LOW}"  # cleanup — built-in default
