id = "jog"
summary = "governs: layer-agnostic bidirectional discrepancy finder; forward check (N→N+1 coverage: bugs) and backward check (N+1→N provenance: points of interest); reading delegated to peer agents (@tend for spec, @rummage for code, user for intent); read-only consultation framing; per-run discrepancy log in .tinker/discrepancies/ | triggers: jog behavior; layer inference; read-only query framing; forward/backward check discipline; discrepancy log output"
description = """
## WHAT

Jog is a layer-agnostic bidirectional discrepancy finder. Given two
adjacent layers, it runs two checks:

- **Forward check** (coverage): is everything in layer N represented
  in layer N+1? Gaps are bugs — intent not captured in spec, or spec
  not implemented in code.
- **Backward check** (provenance): does everything in layer N+1 trace
  back to layer N? Orphans are points of interest — code with no spec
  claim, or spec claims with no identifiable user intent.

**Layer pairs.** Jog works either adjacency:
- Intent ↔ Spec
- Spec ↔ Code

Jog infers which layers to compare from context.

**Reading mechanisms.** Jog never reads a layer directly. It delegates:
- **Intent**: interrogate the user.
- **Spec**: send a read-only `@tend` query.
- **Code**: send a read-only `@rummage` query.

Every `@tend` and `@rummage` message jog sends during a run is
explicitly framed as read-only — "tell me what you see here, don't
act on it." No writes, no fixes, no commissions during a jog run.

**Output.** Each run produces a new file under `.tinker/discrepancies/`
documenting the findings, separated into forward and backward checks.

**Invocation.** On-request for now. Automated triggering is deferred.

## WHY

Spec↔code drift from large refactors surfaces by accident. No existing
agent sweeps an adjacency systematically — rummage investigates when
pointed at something specific; tend handles specific questions, not
full-layer comparison passes. Layer-agnostic design means the same
discipline applies regardless of which adjacency is under scrutiny.

## SCOPE
- In scope: bidirectional layer comparison across either adjacency;
  read-only delegation to peer agents; per-run discrepancy log;
  on-request invocation.
- Out of scope: fix commissioning (jog documents, doesn't dispatch);
  automated triggering (deferred); writing goals or code directly.
"""
parent_id = ""

[[related]]
id = "tend"
reason = "Jog sends read-only @tend queries to read the spec layer during discrepancy runs — no goal edits, no interview."

[[related]]
id = "rummage"
reason = "Jog sends read-only @rummage queries to read the code layer during discrepancy runs — no investigation, no dispatch."

[[related]]
id = "shared-language"
reason = "Jog's output (discrepancy log and conversation) is user-facing; `shared-language` governs its vocabulary."

[[related]]
id = "coding-standards"
reason = "This goal produces code, which follows `coding-standards`. Asymmetric: packaged ancestor-scope standard, no back-link."

[[related]]
id = "peer-consult"
reason = "Jog orchestrates via @-messages to peer agents; the @-block protocol and delivery rules are governed by `peer-consult`."
