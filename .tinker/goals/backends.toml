id = "backends"
kind = "feature"
summary = "This goal owns the pluggable LLM backend abstraction — two implementations (opencode via ephemeral agents/-directory file, and claude via its system-prompt mechanism) that deliver identical system prompt content (the goal's spec plus the framework preamble defined by goal-agents) with the trigger reason as the first turn message, per-backend model-tier defaults, the claude high-tier pin to claude-opus-4-8, and re-injection of CLI error output into the session."
description = """
## WHAT
Tinker runs against a pluggable backend — the LLM CLI it shells out to
— chosen at startup. Two backends exist today: **opencode** (the
default) and **claude** (selected with `--claude`). Both are
co-equal implementations of the same backend abstraction, wired at the
application entry point; business logic depends on the abstraction,
not on any vendor's CLI.

**System prompt.** Both backends deliver an identical system prompt to
every goal agent session — the framework preamble (owned by
`goal-agents`) plus the goal's full description and neighbor table.
The trigger reason is sent as the first turn message. The assembled
system prompt string is identical across backends for goal agents;
only the delivery mechanism differs. Tend is intentionally excepted:
it takes a different system prompt path on each backend.

**opencode** writes the system prompt to an ephemeral file in the
project-local `agents/` directory for each session, invokes opencode
with that file as the agent, and deletes the file when the session
ends (via drop). Session IDs returned by opencode are stored for
subsequent turns.

**claude** maps roles to `claude-opus-4-8` (tend, high tier) and
`sonnet` (goal sessions, mid tier) — the high tier uses a pinned full
model ID rather than the short alias, which can silently resolve to an
unvalidated version. It delivers the system prompt via claude's
system-prompt flag, resumes sessions by session ID, and parses
streaming output.

Both backends rely on prompt-level compliance with the file-access
boundaries their personas state; mechanical guardrails are slated to
be reintroduced through the prompts.

Which model fills each tier is overridable at runtime through
`model-config`; the per-backend mappings above are the built-in
defaults.

**Error feedback.** When a CLI process exits with an error, the backend
captures its output and re-injects it as a message into the active
session, so the agent can read and respond — route to a peer, retry a
transient failure, or report the obstacle rather than dying silently.

Startup argument handling is governed by `startup-args`. Recognised
flags: `--claude` (select the claude backend) and
`--tend-full-goal-context` (restore full goal text in tend's context
instead of the compact index).

## WHY
Tinker's business logic should depend on a backend abstraction, not on
one vendor's CLI: the user picks models at startup, and a new backend
slots in by implementing the abstraction rather than by editing the
system around it.

Delivering the same system prompt on both backends ensures agents
behave consistently regardless of backend choice — goal instructions
in system-prompt position carry the authority the model treats as
foundational, rather than being reasoned away from as a user message.

Re-injecting errors closes the loop — a CLI failure becomes a signal
the agent can hear and course-correct from, rather than silent process
death.

"""
parent_id = ""

[[children]]
id = "model-config"
reason = "user-editable .tinker/config.toml overriding model tier assignments per backend — six slots, absent slots fall back to built-in defaults"

[[related]]
id = "startup-args"
reason = "`startup-args` owns unknown-arg detection, help text, and exit behaviour. The flag set it validates against is defined by this goal (--claude, --tend-full-goal-context, --default-model); when a new flag is added here it must also be registered in `startup-args`."

[[related]]
id = "coding-standards"
reason = "This goal produces code, which follows `coding-standards` (capability-based DI, the polymorphism ladder, `test_spec_`/`test_security_` marking, security.md, and the `tinker-test-case:` convention). Pull the full standard before writing or restructuring this goal's code. Asymmetric: `coding-standards` is a packaged ancestor-scope standard and carries no back-link."

[[related]]
id = "agent-liveness"
reason = "agent-liveness defines the progress guarantee this goal's harness mechanisms enforce: tool-error surfacing makes CLI errors visible so agents can reason about them; silence detection prompts agents that produce no output to take a step."

[[related]]
id = "goal-agents"
reason = "goal-agents defines the uniform session registry and unified runner task this goal's backend abstraction must support; per-tier model resolution for every goal agent is wired through the backend at the application entry point. Changes to the registry or the tier field affect what the abstraction must provide. goal-agents also owns the framework preamble text that forms part of the system prompt both backends deliver."

[[related]]
id = "peer-consult"
reason = "peer-consult's envelope extraction and no-drop delivery guarantee operate on the finalized output this goal's backends deliver; when output buffering or delivery changes, the complete-output invariant peer-consult's scanning depends on must be preserved."
