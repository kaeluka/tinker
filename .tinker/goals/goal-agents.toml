id = "goal-agents"
kind = "feature"
summary = "This goal owns the agent-architecture runtime: the uniform per-goal session registry, universal dispatch routing, the runtime-injected framework preamble and dispatcher-provided spec-diff init section, the cache-stable system-prompt ordering that places session identity last for prefix-cache stability, the tier field and its kind-aware session-start resolution (behavior goals default to high, feature goals to mid), the unified event stream, the .tinker/ access prohibition, tend's two structural exceptions (eager start with TUI-suppressed startup output and the goal-file parse-correction loop), and the startup diagnostic that emits cross-tier goal collision warnings."
description = """
## WHAT
Tinker's agent architecture follows one rule: **every goal is a
structurally identical, process-scoped, addressable agent.** The
session registry maps each goal-id to the sending half of that
agent's message channel; the agent's goal and its resumable LLM
session id live in the persistent per-goal loop the runtime spawns,
not in the registry. `send_message` is the primary dispatch path for
agent-to-agent messaging; `spawn-session` is the sub-session spawning
surface. Both deliver to the same session registry.

Tend has exactly **two structural exceptions**, and no others are
justified — everything else is identical across all agents:
1. **Starts eagerly.** Tend starts on process init so it is ready
   before the user types anything. The TUI suppresses tend's response
   to this startup message in the conversation pane — tend produces
   output but the user sees canned startup text instead. All other
   sessions start lazily on first dispatch and receive no
   startup message.
2. **Handles goal-file parse corrections** — when a goal write
   produces a malformed file, tend is re-queued with correction
   instructions (up to two attempts). No other agent triggers this
   loop because no other agent writes goal files.

**Framework preamble.** Before its goal description, every session
receives a standard runtime-injected preamble; its authoritative text
lives in the runner, not here. It covers goal-tree navigation (the
compact index plus send_message to tend for full text — the goal IS the agent's
role and operating instructions); the send_message tool for
inter-agent messaging and actor-model semantics; the
tend/rummage split along intent/*should* vs code/*is*
(`the-cycles`' producer/skeptic axis); the progress guarantee
(`agent-liveness`); and the neighbor-consultation mandate
(`agent-collaboration`). The one rule this goal owns directly: on
completing significant work an agent reports to its dispatcher — what
it did, decisions beyond the goal, how to try the result, and every
`test_spec_` function it touched.

**Cache-stable prompt ordering.** The system prompt is ordered for
prefix-cache stability: content that is identical between a permanent
goal and its ephemeral sub-sessions — the compact index, framework
preamble, goal description, and neighbors section — comes first;
session-identifying information (the goal id) comes last. This
maximizes the byte-identical prefix that the API provider caches, so
a fresh sub-session reuses its parent's cached prefix. Task-specific
content (the spec diff, the subgoal) arrives in the first user
message, not the system prompt, so it does not affect the cacheable
prefix.

**Dispatcher-provided init section.** When tend dispatches a goal
agent after a goal write, it augments the init message with one
additional section beyond the standard preamble and goal text:

- **Spec diff** — what was removed from the goal text and what was
  added, computed from the old spec and new spec tend holds.

Goal agents needing a code inventory of what currently exists in
their scope should send_message to rummage.

**Tier.** Every goal TOML may carry an optional `tier` field:
`"high"`, `"mid"`, `"low"`, or absent. Absent defaults to `"high"` for
behavior goals (complex tasks — reviewing, summarizing, understanding —
benefit from the strongest model) and `"mid"` for feature goals. Resolved
at session start via `model-config`.

**Startup diagnostics.** At session-registry population, emit a
system message for each goal id found in multiple discovery tiers
(project-local, ancestor global, binary-relative packaged), listing
the contributing tiers and paths.

**Unified event stream.** Every session emits three kinds of event:
a streamed output chunk, a done signal, and a cleanup-blocked signal
carrying the dirty files.

Sessions never read or write `.tinker/goals/`, `.tinker/notes/`, or
`.tinker/state/` (tend-owned); goal content reaches a session via
send_message to tend and the init message (see `no-peek`).

## WHY
A uniform registry eliminates the distinction between "named agents"
(tend, rummage, jog) and "goal sessions." Every goal is a peer:
reachable by the same syntax, governed by the same lifecycle,
producing output in the same event stream.

The two tend exceptions are real: the user must have something to
talk to on startup, and goal-file integrity requires a corrective loop
after writes. Tend starts eagerly precisely because the user needs it
ready; the TUI suppresses the startup response and shows canned text
instead. No other exceptions are justified and none should be added
without explicit user confirmation that they belong in the architecture.

The spec diff closes the information gap that makes large refactors
fail: without it, agents receive only the desired end state and must
infer what to remove — inference that fails systematically for
deletions and the deletion half of every change. Code inventory is
available on demand from rummage.

The structured summary and `test_spec_` listing exist because a
session is an unreliable narrator about its own work — the summary
is the one surface a reviewer actually reads to confirm spec
compliance. Requiring the session to account for every
`test_spec_` function systemically enforces the tests-as-guardrails
standard without relying on the session's general good behavior.

The startup diagnostic exists because cross-tier goal collisions are
rare but invisible: a user installing a global packaged goal that
accidentally shadows a project-local goal would see no warning
without it. Listing the contributing tiers and paths makes the
collision legible and actionable.

The cache-stable ordering exists because each sub-session sends a
fresh system prompt on its first API call. When the system prompt
shares a prefix with its parent's, the API provider caches that
prefix — so the cost of re-sending it shrinks toward zero as the
prefix grows. Placing the goal id — the sole element that differs in
the system prompt between a permanent session and its ephemeral
children — last maximizes that prefix.

"""
parent_id = "the-cycles"

[[children]]
id = "agent-liveness"
reason = "progress guarantee — every agent must advance on error; the behavioral contract delivered as a framework preamble section"

[[children]]
id = "agent-collaboration"
reason = "neighbor consultation mandate — every agent announces significant work to adjacent goals and collaborates to resolve conflicts before and during execution"

[[children]]
id = "cleanup-hook"
reason = "pre-session cleanup — removes tinker-test-case: investigation markers before each goal session runs"

[[children]]
id = "parallel-goal-agents"
reason = "execution model — concurrent agents in shared main tree, coordination-based conflict avoidance via agent-collaboration"


[[related]]
id = "goal-storage"
reason = "goal-storage defines the four-tier goal discovery model (project-local, ancestor global, packaged, binary-relative-packaged) and the four-level precedence rule; goal-agents consumes goal-storage's load result to populate the registry and emit cross-tier collision diagnostics. The tier field extends the goal TOML schema goal-storage governs."

[[related]]
id = "tui"
reason = "the session registry, unified event stream, and tier field change what the TUI renders; /<goal-id> switching, full ownership of the suppression of tend's startup output (the sole suppression mechanism, now that the startup-silence instruction is gone), and the detail-pane tier/kind tag are governed by tui."

[[related]]
id = "backends"
reason = "the session registry and unified runner task change the session management that backends' runner trait must support. The per-tier model resolution path for arbitrary goal agents must be wired at the composition root."

[[related]]
id = "tend"
reason = "tend is the only agent with structural exceptions in the registry (eager start + TUI-suppressed startup output; parse-correction loop). Those two exceptions and nothing more are what make tend different from any other session entry."

[[related]]
id = "coding-standards"
reason = "This goal produces code, which follows coding-standards (capability-based DI, the polymorphism ladder, test_spec_/test_security_ marking, security.md, and the tinker-test-case: convention). Pull the full standard before writing or restructuring this goal's code. Asymmetric: coding-standards is a packaged ancestor-scope standard and carries no back-link."

[[related]]
id = "implementation-ownership"
reason = "implementation-ownership's source-ownership mandate (override the LLM structural taboo; permission to demolish and rewire without hesitation) rides in the framework preamble and session init this goal constructs, injected alongside the goal text."

[[related]]
id = "no-peek"
reason = "This goal's .tinker/ read/write prohibition (sessions never read goal files; content arrives via send_message to tend and the init message) is the agent-architecture expression of no-peek's rule that agents access peer context through send_message, never by reading other goals' files. Asymmetric: no-peek is a packaged goal and carries no back-link."

[[related]]
id = "tend-introspection"
reason = "tend-introspection records observables of this goal's session lifecycle (dispatched/started/finished), send_message dispatch emissions, and the session-start framework-preamble plus goal-list hash; its capture schema depends on what goal-agents' registry emits."

[[related]]
id = "readme-maintainer"
reason = "readme-maintainer's 'The model' section derives its content from this goal's architecture — every goal is an agent, goal-agent dispatch, actor model; when this goal's architecture changes, that section must update."

[[related]]
id = "tier-edit"
reason = "tier-edit owns the TUI keybinding that mutates the tier field this goal defines — the interaction surface for the value this goal's tier semantics govern."

[[related]]
id = "fresh-agents"
reason = "fresh-agents adds an ephemeral entry type to the session registry — sub-sessions that appear during a batch and are retired on batch-start; the session lifecycle this goal governs applies to both permanent and ephemeral entries."

[[related]]
id = "prompt-storage"
reason = "prompt-storage owns the raw prompt text this goal's builder functions assemble into messages — session init templates, framework preamble constants, delivery messages, silence nudges, and parse-correction prompts are loaded from prompt-storage rather than hardcoded."

[[related]]
id = "send-message"
reason = "send-message validates targets against goal-agents' session registry and goal tree, firing recipient sessions immediately into the registry. The registry identifies running sessions; the goal tree identifies known goals the tool can lazy-spawn."

[[related]]
id = "spawn-session"
reason = "spawn-session fires the new sub-session immediately into goal-agents' session registry; the dispatched session is visible in the registry alongside the dispatching session and participates in batch accounting."

[[related]]
id = "recursive-subdivision"
reason = "recursive-subdivision runs within this goal's session registry and lifecycle; each recursive dispatch creates a new sub-session entry, exercising the full depth the registry supports."


