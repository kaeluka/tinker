id = "the-false-promise"
summary = "governs: foundational diagnosis; cognitive debt from AI laundering implementation as quasi-spec; necessity of durable intent layer (goals) separate from code | triggers: architecture questions; why goals exist; why tinker separates intent from implementation"
description = """
## WHAT
The foundational diagnosis that justifies tinker's architecture.
Current coding assistants promise developers they no longer have to
write code, but their architecture forces developers to keep reading
it.

## WHY
Because traditional assistants only output source code, they launder
their own ad-hoc implementation guesses into a "quasi-specification."
Source code conflates human intent with AI scaffolding, and once a
session restarts the two are indistinguishable. This forces a fatal
choice on the user: either vet every line and lose the speedup, or
trust blindly and accumulate paralyzing cognitive debt. Tinker
rejects the choice by extracting intent into a separate, durable
layer — Goals — kept distinct from implementation.

## SCOPE
- In scope: the diagnosis of cognitive debt; the concept of
  quasi-specification laundering; the necessity of extracting intent
  into a durable layer (Goals).
- Out of scope: the mechanics of the dialectic (handled by
  `creative-process`); the specific boundary of who holds what state
  (handled by `user-system-interaction`).

"""
parent_id = ""

[[children]]
id = "creative-process"
reason = "the dialectical process converting tacit user intent into explicit goal conventions through iterative interview"

[[children]]
id = "user-system-interaction"
reason = "architectural boundary: user holds the should (intent), tinker holds the is (state) — governs what surfaces to the user"
