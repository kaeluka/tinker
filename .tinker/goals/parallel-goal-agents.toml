id = "parallel-goal-agents"
summary = "governs: concurrent goal agent execution in shared main working directory; conflict avoidance via agent-collaboration consultation mandate; cleanup-hook runs in main tree before each agent's first turn | triggers: parallel agent execution; conflict avoidance between concurrent agents; dispatch throughput"
description = """
## WHAT

Goal agents run directly in the main working directory. Multiple agents can be active concurrently. Agents avoid conflicts themselves via the consultation mandate in `agent-collaboration`.

## WHY

Coordination via `@`-messages keeps the model simple: no merge infrastructure, and the conflict-resolution protocol agents already use for goal-level coordination doubles as the parallel-write guard.

"""
parent_id = "goal-agents"

[[related]]
id = "agent-collaboration"
reason = "agent-collaboration's consultation mandate is the conflict-avoidance mechanism for concurrent agents in the shared main tree — agents announce work to neighbors and resolve conflicts before proceeding."

[[related]]
id = "cleanup-hook"
reason = "The cleanup hook runs in the main working tree before each agent's first turn."
