id = "tend-introspection"
kind = "feature"
summary = "This goal owns tend's runtime introspection substrate — the append-only event log at .tinker/logs/runtime.jsonl capturing turns, messages, goal-session lifecycle, dispatch, cleanup, goal-file changes, TUI state, batch state transitions, per-API-call token usage, and cross-tier goal collisions, plus the semantic UI state snapshot at .tinker/state/runtime.json, written async and non-blocking; batch transitions also emit as user-visible system messages."
description = """
## WHAT
A runtime introspection substrate that lets tend inspect its own
state — the goal-session queue, the current UI snapshot, message
history — instead of estimating from inference or asking the user
to read numbers off.

The mechanism is **hybrid**: an append-only **event log** for
sequence and history, plus a **state file** holding the current
semantic UI snapshot. Both live under
`.tinker/`. Only tend reads the substrate, and only to observe, not
to act.

The **event log** captures: tend turn boundaries; message-level
events in three categories — user input (full text), assistant
reply (full text, tool calls and results inline), and
tend-injected system messages (full content plus a `source` field
naming the emitting component — dispatcher, harness);
goal-session lifecycle (dispatched, started, finished) with
observable facts; dispatch emissions; cleanup-hook results;
per-API-call token counts (prompt, completion, total, and provider-specific extensions like cached tokens) tagged with the originating goal; goal-file changes; TUI structural state changes (selection,
focus, scroll, queue); and **batch state transitions** — when the
system moves from idle to active and back — as first-class events,
alongside the existing system-message stream. At session registry
population, cross-tier goal collisions (same id found in multiple
discovery tiers) are captured with the contributing tiers and paths,
paralleling the user-visible system message emitted by goal-agents. The session-start event captures the
system-prompt text plus a goal-list hash so persona/goal drift is
detectable across sessions. The log format carries a `kind` discriminator, supports
`jq`-style aggregation, and lets tend retrieve a single event at low
token cost. The log appends forever — no rotation; the user
clears it manually if it grows. On startup, token-usage events are
replayed from the log to reconstruct per-goal totals, seeding both
the state snapshot and the TUI's in-memory metrics — so historic
data survives restarts and deleting the log is the reset action.
Specific event-kind names, shape, and retrieval tooling are
implementation's call within these constraints.

Capture stops at the message level; tend's own dialectic moves stay
in pane content and `notes.md`.

Finished-session events carry **observable facts only** — no
session-declared outcome. Implementation chooses the fields.

The **state file** is JSON, semantic only: selected goal id, focus
region, scroll offsets, queue snapshot (running + pending with
reasons and start/queue timestamps), and per-goal token-usage metrics
(session count, average input tokens per session, average output
tokens per session) derived from the logged per-call data.

Writes are **async and non-blocking**, so the substrate never
slows tend's main loop.

## WHY
Tend has been operating on inferred state — asking the user
"what's running?", reconstructing turn counts from memory. That's
backwards: tend itself holds this state. The
`user-system-interaction` principle says tinker holds the *is*,
but until now the *is* reached only the user's view, never tend's
own reasoning. This goal closes that asymmetry — tend is part of
tinker, and the *is* should be available to it the same way it's
available to the user.

The persistent log also opens cross-session self-improvement.
Every session's transcript, timing, queue events, and observable
goal-session facts become durable, so questions like "why did the
previous session need a restart?" and "did my reactive scheduling
produce real work?" become structured queries instead of fading
memory. Logging system messages as a first-class category is what
makes the "why" of each turn queryable — they explain why turns
happened, not just what was said.

Finished-session events carry observables rather than a
self-declared outcome because the session is an unreliable
narrator about its own work. Self-classification fails exactly
where it matters most — scope expansion, scope shrinkage, doing
something else entirely. Observable facts from outside the session
are trustworthy where the session's own account is not.

Because the log stores message text verbatim — tool inputs and
outputs included — redacting anything sensitive is the user's
responsibility.

"""
parent_id = "user-system-interaction"
children = []

[[related]]
id = "coding-standards"
reason = "This goal produces code, which follows `coding-standards` (capability-based DI, the polymorphism ladder, `test_spec_`/`test_security_` marking, security.md, and the `tinker-test-case:` convention). Pull the full standard before writing or restructuring this goal's code. Asymmetric: `coding-standards` is a packaged ancestors-scope standard and carries no back-link."

[[related]]
id = "tui"
reason = "tend-introspection's substrate captures tui's structural state (selection, focus, scroll offsets, queue snapshot) in its state file and logs tui state-change events; the set of observable structural state it records is defined by what tui renders and maintains. The state snapshot also carries per-goal token-usage metrics that tui renders as cost-driver indicators in the goal-list rows."

[[related]]
id = "goal-agents"
reason = "tend-introspection logs goal-session lifecycle events (dispatched/started/finished), dispatch emissions, a session-start system-prompt and goal-list-hash capture, and cross-tier goal collision events (goal id, contributing tiers and paths) — all produced by goal-agents' session registry and its startup diagnostics. Per-API-call token counts are tagged with the originating goal via the session registry."

[[related]]
id = "goal-storage"
reason = "Your runtime event log captures goal-loading events including discovery and precedence resolution; goal-storage owns the four-tier model (project-local, ancestor global, packaged, binary-relative-packaged) that determines what gets loaded. Asymmetric: goal-storage is a packaged goal and cannot carry a back-link."

[[related]]
id = "fresh-agents"
reason = "fresh-agents owns the batch concept (active/idle system state and the retirement trigger); tend-introspection captures batch state transitions as first-class event log entries and system messages."

[[related]]
id = "spawn-session"
reason = "spawn-session dispatches produce a new event type in the runtime event log — dispatcher (parent) session, label, sub-session id, dispatch time."

[[related]]
id = "send-message"
reason = "send-message dispatches produce a new event type in the runtime event log — sender, target, dispatch time."

[[related]]
id = "backends"
reason = "Per-API-call token counts originate at the backend's tool-loop seam where each API response is parsed; this goal logs them and derives per-session metrics for the state snapshot."


