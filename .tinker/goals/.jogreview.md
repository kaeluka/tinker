# Goal tree staleness review — 2026-06-04

Produced by @jog. Full log: `.tinker/discrepancies/2026-06-04-goal-tree-staleness.md`

---

## Medium-signal findings

**F1 — `the-cycles` narrows jog's scope.**
`the-cycles` places jog at the intent↔spec backward adjacency only. Jog's own goal, `goal-complementarity`, and `tend`'s edge reason all describe jog as a bidirectional finder across any adjacent layers — including spec↔code via @rummage delegation. `the-cycles`'s characterization predates jog's current scope and should be updated.

**F2 — `goal-agents` prohibition vs. `tend-introspection` reads.**
`goal-agents` prohibits all sessions from reading `.tinker/state/`. `tend-introspection` describes tend reading `.tinker/state/runtime.json`. The two structural exceptions `goal-agents` already names for tend (eager start + parse-correction loop) do not include this. Either `goal-agents` needs a third explicit tend exception, or `tend-introspection` is describing something that was never sanctioned.

**B1 — `.tinker/notes/` is referenced but owned by no goal.**
`goal-agents` and `rummage` both prohibit writes to `.tinker/notes/`. `tend-introspection` references a `notes.md` there for tend's dialectic. No goal defines this path or its purpose. Either a vestigial reference to an abandoned design, or a missing goal.

---

## Lower-signal findings

- **`backends` flag count inconsistency.** The goal body lists two backend-selection flags (`--claude`, `--tend-full-goal-context`); an edge reason in `startup-args` lists three, adding `--default-model`. One source is stale.
- **`tier-edit` edge reason truncated.** The related-link reason for `goal-agents` in `tier-edit` describes tier semantics incompletely relative to what `goal-agents` actually specifies.
- **`backends` model pin possibly outdated.** The high-tier claude pin is listed as `claude-opus-4-8`; worth verifying this is still the current model identifier.
- **`user-persona` TOML style inconsistency.** Minor formatting inconsistency relative to other goal files.
